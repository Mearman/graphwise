import { style } from "@vanilla-extract/css";

export const container = style({
	width: "100%",
	height: "400px",
	position: "relative",
});

export const gridLines = style({
	pointerEvents: "none",
});
